<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Deliberately offline: no INTERNET permission. The vault never syncs. -->

    <!-- allowBackup/fullBackupContent cover pre-Android-12 backup. On Android 12+
         (targetSdk 31+) backup AND the newer device-to-device transfer are governed by
         dataExtractionRules; "@null" left device-transfer at its permissive default, so the
         encrypted vault could be migrated off-device. Point at explicit deny rules instead. -->
    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:label="vaultis"
        android:supportsRtl="true"
        android:theme="@style/Theme.Vaultis">
        <!-- Handle config changes in-process instead of recreating the Activity.
             Compose redraws on these without losing state; recreation would
             otherwise discard the in-flight clipboard auto-clear timer (App.kt). -->
        <activity
            android:name="com.vaultis.MainActivity"
            android:exported="true"
            android:label="vaultis"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboard|keyboardHidden|navigation|touchscreen|uiMode|locale|layoutDirection|fontScale|density|mcc|mnc"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
